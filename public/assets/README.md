# Assets

This folder is reserved for future static assets such as exported icons, screenshots, or approved brand imagery.

The current demo uses FlagCDN URLs for country flags and does not require bundled image assets.
